<div class="col-md-3 topic-panel">
    <h2><%= link_to "##{topic.title}", topic_path(topic) %>
    <small><%= link_to "", topic, method: :delete, class: 'glyphicon glyphicon-trash' %></small></h2>
    <% for b in topic.bookmarks %>
    <%= render b %>
    <% end %>
</div>