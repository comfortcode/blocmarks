    <!-- Fixed navbar -->
    <div class="navbar navbar-default"> 
    <!--can add class ^ navbar-fixed-top-->
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <!--<a  href="#"><b>Blocmarks</b></a>-->
        <%= link_to "Mylinks", root_path, class: "navbar-brand" %>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
         <% if current_user %>
            <li><%= link_to "My Links", topics_path %></li>         
            <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
            <!--<li> Hello <%#= (current_user.name || current_user.email)%>!</li>-->
         <% else %>
        <li><%= link_to "Already a member?", new_user_session_path %></li>
         <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
